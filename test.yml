---
- hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Debug
      debug: var=inventory_hostname

    - name: vmware drs
      vmware_drs:
        hostname: "vcenter.example.com"
        username: "username"
        password: "password"
        name: "hosta-hostb"
        hosts:
          - "hosta"
          - "hostb"
